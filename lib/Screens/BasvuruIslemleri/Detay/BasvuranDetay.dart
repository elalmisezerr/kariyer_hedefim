import 'package:flutter/material.dart';

class BasvuranDetay extends StatelessWidget {
  const BasvuranDetay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
